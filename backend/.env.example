# Flask Configuration
# CRITICAL: Set a strong, random SECRET_KEY for production
# Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
# In development, a random key is auto-generated if not set
SECRET_KEY=your-secret-key-here-replace-with-random-64-char-hex

# Flask Environment (development or production)
# IMPORTANT: Defaults to 'production' if not set
FLASK_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# In development, defaults to localhost:3000,localhost:5000
# In production without CORS_ORIGINS set, CORS is DISABLED (same-origin only)
CORS_ORIGINS=

# Rate Limiting Configuration
# Storage backend for rate limiting (optional)
# Default: memory:// (in-memory, resets on restart)
# For production with multiple workers, use Redis:
# RATE_LIMIT_STORAGE_URI=redis://localhost:6379
RATE_LIMIT_STORAGE_URI=memory://

# Google OAuth Configuration (for Anthropic passthrough)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Note: For Google OAuth to work, you need to:
# 1. Create a project in Google Cloud Console
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add http://localhost:5000/api/auth/google/callback to authorized redirect URIs

# Error Tracking with Sentry (optional)
# Get your DSN from https://sentry.io/ after creating a project
# Leave empty to disable Sentry integration
SENTRY_DSN=
# SENTRY_DSN=https://your-public-key@o123456.ingest.sentry.io/7654321

# Sentry performance sampling rate (0.0 to 1.0)
# 0.1 means 10% of transactions are traced
# Reduce for high-traffic applications to save on quota
SENTRY_TRACES_SAMPLE_RATE=0.1

# Sentry release tracking (optional)
# Format: app@version (e.g., sailor@2.0.0)
SENTRY_RELEASE=sailor@2.0.0

# Prometheus Metrics
# Set to 'false' to disable metrics collection and /metrics endpoint
# Enabled by default for monitoring and observability
ENABLE_METRICS=true