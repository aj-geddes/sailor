# Railway Environment Variables for Sailor MCP Server
# Copy these to your Railway project settings

# Server Configuration
# PORT is automatically set by Railway - do not override
# HOST=0.0.0.0  # Default, no need to set

# Logging
SAILOR_LOG_LEVEL=INFO

# Rate Limiting (adjust based on your needs)
RATE_LIMIT_REQUESTS=100     # Max requests per window per client
RATE_LIMIT_WINDOW=60        # Rate limit window in seconds
RATE_LIMIT_RENDER=20        # Max render requests per window (rendering is expensive)

# Transport Configuration (Railway uses Streamable HTTP)
MCP_TRANSPORT=http
MCP_TRANSPORT_TYPE=streamable-http

# Note: SSE transport is for local development only
# Railway deployment ONLY supports Streamable HTTP

# Optional: Playwright configuration
# PLAYWRIGHT_BROWSERS_PATH is set in Dockerfile
